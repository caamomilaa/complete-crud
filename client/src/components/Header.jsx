import { StyledHeader, StyledTitle } from './header.styles';

const Header = () => {
	return (
		<>
			<StyledHeader>
				<StyledTitle>Users Dashboard</StyledTitle>
			</StyledHeader>
		</>
	);
};

export default Header;
